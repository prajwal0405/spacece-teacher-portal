import mongoose from 'mongoose'; import { ActivitySubmission } from './src/models/ActivitySubmission.js'; async function run() { try { await mongoose.connect('mongodb://127.0.0.1:27017/teacher_training_portal'); const docs = await ActivitySubmission.find().sort({_id:-1}).limit(2).populate('teacher', 'name').populate('mentor', 'name'); console.log(JSON.stringify(docs, null, 2)); } catch(e) { console.error(e) } finally { await mongoose.disconnect(); } } run();
