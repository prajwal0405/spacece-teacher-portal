import mongoose from 'mongoose'; import { ActivitySubmission } from './src/models/ActivitySubmission.js'; async function run() { try { await mongoose.connect('mongodb+srv://spacece_app_user:spaceceindiafoundation@spacece-newsletter.mtyenck.mongodb.net/spacece_teacher_training?retryWrites=true&w=majority&appName=spacece-newsletter'); const docs = await ActivitySubmission.find().sort({createdAt:-1}).limit(2).populate('teacher', 'name').populate('mentor', 'name'); console.log(JSON.stringify(docs, null, 2)); } catch(e) { console.error(e) } finally { await mongoose.disconnect(); } } run();
